#!/usr/bin/make -f

%:
	dh $@

override_dh_auto_configure:
	qmake DeepinVentoyInstaller.pro PREFIX=/usr

override_dh_auto_build:
	lrelease DeepinVentoyInstaller.pro
	make

override_dh_auto_install:
	make install INSTALL_ROOT=$(CURDIR)/debian/deepin-ventoy-installer
